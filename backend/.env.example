# MongoDB
MONGO_URI=mongodb+srv://<DB_USERNAME>:<DB_PASSWORD>@cluster0.nmzd3.mongodb.net/<DB_NAME>?retryWrites=true&w=majority

# Auth
JWT_SECRET=<JWT_SECRET>
JWT_EXPIRES_IN=7d

# Server
PORT=5000
CLIENT_URL=http://localhost:5173

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=<SMTP_USER>
SMTP_PASS=<SMTP_PASS>
EMAIL_FROM=CampusEvents <no-reply@example.com>
